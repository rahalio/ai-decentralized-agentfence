/**
 * ExecuteArmKillSwitch - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: kill-switch
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteArmKillSwitch Use Case
 *
 * DDD: Application use case for get Arm.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ArmKillSwitchInput, ArmKillSwitchOutput } from "../dto/arm.dto";
import type { ExecutionContextService, IdGeneratorService } from "@agentfence/services/_shared/index.js";
import type { ArmRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteArmKillSwitch {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly arm: ArmRepository
  ) {}

  async execute(input: ArmKillSwitchInput): Promise<ArmKillSwitchOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.kswId();

    // All field names extracted from OpenAPI spec: approverId
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).orderId) {
      throw new ValidationError("orderId is required");
    }
    const orderId = (input as any).orderId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.arm.armKillSwitch({ orderId: orderId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${orderId}`);
    }

    return entity as ArmKillSwitchOutput;
  }
}
