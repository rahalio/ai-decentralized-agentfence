/**
 * MarketplaceAdapters DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: marketplace-adapters
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * MarketplaceAdapters Domain Module - Composition root
 *
 * DDD: Composition root for marketplace-adapters domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  HaltAckRepositoryAdapter,
  MarketplaceAdapterRepositoryAdapter,
  PartnerAttestationRepositoryAdapter,
} from "@agentfence/adapters/marketplace-adapters";
import { getIdGeneratorService } from "@agentfence/adapters";
import type { AdapterDynamoDBClient } from "@agentfence/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteAcknowledgeHalt,
  ExecuteGetMarketplaceAdapter,
  ExecuteListMarketplaceAdapters,
  ExecuteRegisterMarketplaceAdapter,
} from "@agentfence/services/marketplace-adapters/usecases";
// Ports (interfaces)
import type {
  HaltAckRepository,
  MarketplaceAdapterRepository,
  PartnerAttestationRepository,
} from "@agentfence/services/marketplace-adapters/ports";

export interface MarketplaceAdaptersDomainModule {
  repos: {
    haltAcks: HaltAckRepository;
    marketplaceAdapters: MarketplaceAdapterRepository;
    partnerAttestations: PartnerAttestationRepository;
  };
  useCases: {
    haltAcks: {
      get: ExecuteAcknowledgeHalt;
    };
    marketplaceAdapters: {
      create: ExecuteRegisterMarketplaceAdapter;
      get: ExecuteGetMarketplaceAdapter;
      list: ExecuteListMarketplaceAdapters;
    };
  };
}

export function buildMarketplaceAdaptersDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): MarketplaceAdaptersDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      haltAcks?: HaltAckRepository;
      marketplaceAdapters?: MarketplaceAdapterRepository;
      partnerAttestations?: PartnerAttestationRepository;
    } = {};

    obj.haltAcks = new HaltAckRepositoryAdapter(dynamoClient);
    obj.marketplaceAdapters = new MarketplaceAdapterRepositoryAdapter(dynamoClient);
    obj.partnerAttestations = new PartnerAttestationRepositoryAdapter(dynamoClient);
    return {
      haltAcks: obj.haltAcks!,
      marketplaceAdapters: obj.marketplaceAdapters!,
      partnerAttestations: obj.partnerAttestations!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    haltAcks: {
      get: new ExecuteAcknowledgeHalt(executionContext, idGenerator, repos.haltAcks),
    },
    marketplaceAdapters: {
      create: new ExecuteRegisterMarketplaceAdapter(executionContext, idGenerator, repos.marketplaceAdapters),
      get: new ExecuteGetMarketplaceAdapter(executionContext, idGenerator, repos.marketplaceAdapters),
      list: new ExecuteListMarketplaceAdapters(executionContext, idGenerator, repos.marketplaceAdapters),
    },
  };
  return { repos, useCases: useCases as MarketplaceAdaptersDomainModule["useCases"] };
}
