/**
 * GetKillSwitchBlastRadius handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: kill-switch
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getKillSwitchBlastRadius } from "@agentfence/api-server/domains/kill-switch/handlers";
import type { KillSwitchDDDDependencies } from "@agentfence/api-server/domains/kill-switch";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@agentfence/tests/shared";
import { KILLSWITCH_USE_CASES } from "../__factories__/use-cases.js";

describe("GetKillSwitchBlastRadius", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: KillSwitchDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<KillSwitchDDDDependencies>(
      KILLSWITCH_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetKillSwitchBlastRadius, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { orderId: entity.id };

    await getKillSwitchBlastRadius(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetKillSwitchBlastRadius.execute).toHaveBeenCalledWith(expect.objectContaining({ orderId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { orderId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetKillSwitchBlastRadius, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getKillSwitchBlastRadius(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
