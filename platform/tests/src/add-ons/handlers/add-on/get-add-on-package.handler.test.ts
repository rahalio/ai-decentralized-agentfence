/**
 * GetAddOnPackage handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: add-ons
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getAddOnPackage } from "@agentfence/api-server/domains/add-ons/handlers";
import type { AddOnsDDDDependencies } from "@agentfence/api-server/domains/add-ons";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@agentfence/tests/shared";
import { ADDONS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetAddOnPackage", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: AddOnsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<AddOnsDDDDependencies>(
      ADDONS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetAddOnPackage, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { addOnId: entity.id };

    await getAddOnPackage(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetAddOnPackage.execute).toHaveBeenCalledWith(expect.objectContaining({ addOnId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { addOnId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetAddOnPackage, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getAddOnPackage(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
