/**
 * GetAuditEvent handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: audits
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getAuditEvent } from "@agentfence/api-server/domains/audits/handlers";
import type { AuditsDDDDependencies } from "@agentfence/api-server/domains/audits";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@agentfence/tests/shared";
import { AUDITS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetAuditEvent", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: AuditsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<AuditsDDDDependencies>(
      AUDITS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetAuditEvent, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { eventId: entity.id };

    await getAuditEvent(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetAuditEvent.execute).toHaveBeenCalledWith(expect.objectContaining({ eventId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { eventId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetAuditEvent, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getAuditEvent(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
