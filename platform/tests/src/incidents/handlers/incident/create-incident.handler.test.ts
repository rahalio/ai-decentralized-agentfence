/**
 * CreateIncident handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: incidents
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { createIncident } from "@agentfence/api-server/domains/incidents/handlers";
import type { IncidentsDDDDependencies } from "@agentfence/api-server/domains/incidents";
import { createIncident as createIncidentEntity } from "@agentfence/factories/incidents";
import { createCreateIncidentRequest } from "@agentfence/factories/incidents";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@agentfence/tests/shared";
import { INCIDENTS_USE_CASES } from "../__factories__/use-cases.js";

describe("CreateIncident", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: IncidentsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<IncidentsDDDDependencies>(
      INCIDENTS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 201 with created entity", async () => {
    const input = createCreateIncidentRequest();
    const entity = createIncidentEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeCreateIncident, "execute").mockResolvedValue(useCaseResult);

    await createIncident(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeCreateIncident.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(201);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
