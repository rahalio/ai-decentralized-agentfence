/**
 * VerifyPartnerAttestation handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: marketplace-adapters
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { verifyPartnerAttestation } from "@agentfence/api-server/domains/marketplace-adapters/handlers";
import type { MarketplaceAdaptersDDDDependencies } from "@agentfence/api-server/domains/marketplace-adapters";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@agentfence/tests/shared";
import { MARKETPLACEADAPTERS_USE_CASES } from "../__factories__/use-cases.js";

describe("VerifyPartnerAttestation", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: MarketplaceAdaptersDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<MarketplaceAdaptersDDDDependencies>(
      MARKETPLACEADAPTERS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeVerifyPartnerAttestation, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { agentId: entity.id };

    await verifyPartnerAttestation(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeVerifyPartnerAttestation.execute).toHaveBeenCalledWith(expect.objectContaining({ agentId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { agentId: nonExistentId };
    
    vi.spyOn(mockDeps.executeVerifyPartnerAttestation, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      verifyPartnerAttestation(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
