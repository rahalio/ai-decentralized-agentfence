/**
 * ListMarketplaceAdapters handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: marketplace-adapters
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { listMarketplaceAdapters } from "@agentfence/api-server/domains/marketplace-adapters/handlers";
import type { MarketplaceAdaptersDDDDependencies } from "@agentfence/api-server/domains/marketplace-adapters";
import { createMarketplaceAdapter as createMarketplaceAdapterEntity } from "@agentfence/factories/marketplace-adapters";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@agentfence/tests/shared";
import { MARKETPLACEADAPTERS_USE_CASES } from "../__factories__/use-cases.js";

describe("ListMarketplaceAdapters", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: MarketplaceAdaptersDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<MarketplaceAdaptersDDDDependencies>(
      MARKETPLACEADAPTERS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with list of entities", async () => {
    const entities = [
      createMarketplaceAdapterEntity({ orgId }),
      createMarketplaceAdapterEntity({ orgId }),
    ];
    const useCaseResult = {
      data: { items: entities, nextCursor: undefined, prevCursor: undefined },
      meta: { correlationId: "test", timestamp: new Date().toISOString(), pagination: {} }
    };
    
    vi.spyOn(mockDeps.executeListMarketplaceAdapters, "execute").mockResolvedValue(useCaseResult);

    await listMarketplaceAdapters(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeListMarketplaceAdapters.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
