# Deprecated monolith.
# Canonical OpenAPI lives under packages/openapi-core/src/ — one YAML file per domain:
#   identity, agents, policies, audits, kill-switch, add-ons,
#   delegations, incidents, reporting, marketplace-adapters
# See PRODUCT.md and packages/openapi-core/.redocly.yaml.
openapi: 3.1.0
info:
  title: Agentfence (moved)
  version: 0.0.0
  description: Specs moved to packages/openapi-core/src/
paths: {}
